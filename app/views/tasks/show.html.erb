<h1>Details of Task - <%= @task.title %></h1>

<p>Completed: <%= @task.completed %></p>
<p>Details: <%= @task.details %></p>

<br>
<%= link_to "Update Task", edit_path %>
<%= link_to "Delete Task", destroy_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
<%= link_to "Back to list", tasks_path %>
